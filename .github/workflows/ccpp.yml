name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: install depends
      run: apt install -y libevent-dev
    - name: make
      run: make DISABLE_SHADOWSOCKS=1
